apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "mi-chart.name" . }}-pvc-config

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.storageSize }}

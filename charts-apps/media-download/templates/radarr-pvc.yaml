apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.radarr.appName }}-pvc-config

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.radarr.pvc.storageSize }}

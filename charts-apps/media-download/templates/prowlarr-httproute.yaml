apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.prowlarr.appName }}-route

spec:
  parentRefs:
    - name: cluster-gateway
      namespace: kube-system

  hostnames:
  - {{ printf "%s.%s" .Values.prowlarr.appName .Values.httpRoute.domain | quote }}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
      - name: {{ .Values.prowlarr.appName }}-svc
        port:  {{ .Values.prowlarr.service.targetPort }}

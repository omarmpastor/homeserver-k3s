apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.jackett.appName }}-svc

spec:
  selector:
    app: {{ .Values.jackett.appName }}
  
  ports:
    - name: {{ .Values.jackett.service.portHttpName }}
      port: {{ .Values.jackett.service.port }}
      targetPort: {{ .Values.jackett.service.targetPort }}
  
  type: {{ .Values.jackett.service.type }}

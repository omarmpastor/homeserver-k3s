apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.bazarr.appName }}-pvc-config

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.bazarr.pvc.storageSize }}

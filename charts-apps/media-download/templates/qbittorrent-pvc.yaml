apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.qbittorrent.appName }}-pvc-config

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.qbittorrent.pvc.storageSize }}

apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.bazarr.appName }}-route

spec:
  parentRefs:
    - name: cluster-gateway
      namespace: kube-system

  hostnames:
  - {{ printf "%s.%s" .Values.bazarr.appName .Values.httpRoute.domain | quote }}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
      - name: {{ .Values.bazarr.appName }}-svc
        port:  {{ .Values.bazarr.service.targetPort }}

apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.prowlarr.appName }}-svc

spec:
  selector:
    app: {{ .Values.prowlarr.appName }}
  
  ports:
    - name: {{ .Values.prowlarr.service.portHttpName }}
      port: {{ .Values.prowlarr.service.port }}
      targetPort: {{ .Values.prowlarr.service.targetPort }}
  
  type: {{ .Values.prowlarr.service.type }}

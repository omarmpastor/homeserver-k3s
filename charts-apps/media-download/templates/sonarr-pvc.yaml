apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ .Values.sonarr.appName }}-pvc-config

spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.sonarr.pvc.storageSize }}

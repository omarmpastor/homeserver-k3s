apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ .Values.jackett.appName }}-route

spec:
  parentRefs:
    - name: cluster-gateway
      namespace: kube-system

  hostnames:
  - {{ printf "%s.%s" .Values.jackett.appName .Values.httpRoute.domain | quote }}

  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /

      backendRefs:
      - name: {{ .Values.jackett.appName }}-svc
        port:  {{ .Values.jackett.service.targetPort }}
